# ═══════════════════════════════════════════════════════════════════════════
# MapAble — Environment Variables
# ═══════════════════════════════════════════════════════════════════════════
# Copy this file to .env.local and fill in the values.
# See docs/ENVIRONMENT.md for the full reference with Vercel setup steps.
#
# Legend:
#   REQUIRED      — must be set in every environment
#   PROD-REQUIRED — must be set in production; optional in local dev
#   OPTIONAL      — feature works without it; typed as string | undefined
# ═══════════════════════════════════════════════════════════════════════════

# ── Database (Neon) ──────────────────────────────────── REQUIRED ────────
DATABASE_URL="postgresql://user:pass@ep-xxx.region.aws.neon.tech/mapable?sslmode=require"

# ── Auth (Clerk) ─────────────────────────────────────── REQUIRED ────────
CLERK_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."

# ── Vercel Deployment ────────────────────────────────── PROD-REQUIRED ───
# VERCEL_PROJECT_ID="prj_..."
# VERCEL_TOKEN="..."

# ── Search (Typesense Cloud) ─────────────────────────── REQUIRED ────────
TYPESENSE_HOST="xyz.a1.typesense.net"
TYPESENSE_API_KEY="your-admin-api-key"
# TYPESENSE_SEARCH_KEY="your-search-only-key"            # OPTIONAL — client-safe
# TYPESENSE_PORT=443                                      # OPTIONAL — default: 443
# TYPESENSE_PROTOCOL="https"                              # OPTIONAL — default: https

# ── Workflows (Inngest) ──────────────────────────────── REQUIRED ────────
INNGEST_EVENT_KEY="your-event-key"
# INNGEST_SIGNING_KEY="your-signing-key"                  # PROD-REQUIRED — webhook verification

# ── Federation SSO ───────────────────────────────────── OPTIONAL ────────
# Configured in Clerk dashboard → Enterprise SSO. These IDs let the app
# identify which SSO connection a login came from.
# CLERK_SSO_DISAPEDIA_CONNECTION_ID="conn_..."            # OPTIONAL — Disapedia OIDC
# CLERK_SSO_ACCESSIBOOKS_CONNECTION_ID="conn_..."         # OPTIONAL — AccessiBooks SAML
# CLERK_WEBHOOK_SECRET="whsec_..."                        # PROD-REQUIRED — Clerk webhook verification

# ── AI ───────────────────────────────────────────────── OPTIONAL ────────
# At least one AI provider key is recommended for chat features.
# OPENAI_API_KEY="sk-..."
# ANTHROPIC_API_KEY="sk-ant-..."

# ── Storage (Vercel Blob) ────────────────────────────── OPTIONAL ────────
# BLOB_READ_WRITE_TOKEN="vercel_blob_..."

# ── Runtime ──────────────────────────────────────────── OPTIONAL ────────
NODE_ENV="development"
